let num=7;
let type= (num % 2=== 0) ? ("even"): ("odd");
console.log(type);

// shortcut for if else
//     let marks=66;

// let grade=  (marks >90) ? "grade a" 
//            : (marks >= 75) ? "grade b"
//             : (marks >= 50) ? "c"
//            : " fail" ;
// console.log(grade);